package org.cnc.qrcode;

import android.app.Activity;
import android.os.Bundle;
import android.widget.TextView;

public class AboutUsScreen extends Activity {
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.useofdqrcode);
		// TextView text = (TextView) findViewById(R.id.textView1);
		//
		// String txt = "Store event"
		// +
		// "\nEvents for the inviter in a shop or inviters who are correct the answer to question about related a store will be shown the next store location."
		// + "\nTreasure Hunt"
		// +
		// "\nThe person who finds a treasure can be seen the next DQR code location."
		// + "\nDating"
		// +
		// "\nThe couple who don't know each other can meet a designated place."
		// + "\nStart and End of work time"
		// + "\nWe can know the DQR code image login time.";
		// text.setText(txt);
	}
}